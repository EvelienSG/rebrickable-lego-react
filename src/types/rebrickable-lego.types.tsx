export interface Theme {
    id: number;
    name: string;
  }
  
export interface Set {
    set_num: string;
    name?: string;
    year?: number;
    num_parts?: number;
    set_img_url?: string;
    favorite?: boolean;
  }
